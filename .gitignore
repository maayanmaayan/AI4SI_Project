.playwright-mcp
.env
.venv
node_modules
.pytest_cache
.ruff_cache
.vscode
__pycache__

# Data files (large, should use DVC or similar for versioning)
data/raw/osm/*
data/raw/census/*
# Keep most processed data ignored, but allow model-ready features to be tracked
# (e.g. target_points.parquet under data/processed/features/...)
data/processed/**
!data/processed/
!data/processed/.gitkeep
!data/processed/**/.gitkeep
!data/processed/features/
!data/processed/features/**

# Model checkpoints (large files)
models/checkpoints/*
!models/checkpoints/.gitkeep

# Experiment outputs
# experiments/runs/*
# !experiments/runs/.gitkeep

# Jupyter notebook checkpoints
.ipynb_checkpoints/

# Python cache
*.pyc
*.pyo
*.pyd
.Python

# IDE
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Local run logs / captured stdout
*.out

# OSMnx cache (Overpass API responses)
cache/

# Local backups (not part of submission / reproducible artifacts)
.backup/
backup/